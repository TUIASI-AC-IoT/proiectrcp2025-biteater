RemoteTree {
    align: center middle;
    text-align: center;
    background: rgba(0,0,0, 0.7);
    width: 1fr;
    height: 1fr;
    background: $surface;
    border: solid $primary;
    padding: 1;
}

Label {
    width: 100%;
    text-align: center;
    background: $accent;
    color: auto;
    margin-bottom: 1;
}